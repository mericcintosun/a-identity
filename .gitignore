# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
**/node_modules
dist
**/dist
dist-ssr
*.local

# Runtime platform state (agent/wallet metadata, instructions) - never commit
mcp/data/

# Scratchpad / recordings
scratchpad/

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env / secrets (blockchain keys, RPC urls, API keys - never commit)
.env
.env.*
!.env.example

# Claude Code machine-local settings
.claude/settings.local.json
.vercel

# Internal partner brief (not for the repo - hand to Aybars directly)
AYBARS-BRIEF.md

# Internal session handoff for the next agent (not for the repo)
SESSION-HANDOFF.md

# Internal security working doc - enumerates live/unpatched gaps; never publish
CRITICAL-GAPS.md

# Local UI-drive screenshots (Playwright); never commit
ui-shots/

# Internal multichain strategy doc (kept local, not for the public repo)
MULTICHAIN-STRATEGY.md

# TypeScript incremental build cache (regenerated by tsc; never commit)
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local-only internal folder: hackathon planning, pitch, decks, marketing images,
# scaffold cruft. Kept on disk, never pushed to the public repo.
_internal/

# OKX 6-day hackathon plan + feature-fit + ideation phase docs (local-only planning;
# already covered by _internal/, listed by name too so a root copy is never pushed)
OKX-6DAY-PLAN.md
OKX-6DAY-FEATURE-FIT.md
FEATURE-IDEATION-PHASE1.md
FEATURE-IDEATION-PHASE2.md

# Meriç's personal task list (local-only, never pushed)
new-tasks.md

# Dev tooling / scaffold artifacts (kept local, not for the public repo)
.agents/
.claude/
install.cmd
skills-lock.json
A-Identity-Architecture.pdf
aidentity-review-video.mp4
A-Identity_Robinhood_Full_Runbook.md
# A bare `.gitignore` used to sit here, and it did far more than ignore this file.
#
# On THIS file the rule was inert: git ignores nothing it already tracks, and the root
# .gitignore has been tracked since the first commit. But a bare pattern has no leading
# slash, so it matches at EVERY depth. soroban/.gitignore, trust-guard/.gitignore and
# soroban/contracts/agent-spend-policy/fuzz/.gitignore were therefore invisible: not
# tracked, and not showing up as untracked either. A fresh clone had no ignore rule for
# target/, *.wasm, mutants.out*/ or dist-test/, and the reasoning behind the wasm
# non-reproducibility policy (written in soroban/.gitignore) reached nobody who cloned.
#
# It also made `git rm --cached .gitignore` a quiet operation on the file that keeps
# .env, .env.* and mcp/data/ out of a public repo, while claiming an intent the repository
# contradicts: this file is public on origin/main, names and all.
#
# Do not add it back. If you ever need to ignore one specific ignore file, anchor it with a
# leading slash and name the path.
A-Identity_Robinhood_Runbook_Site.html

# Checkpoint screenshots (local only, for HackQuest submission)
_checkpoint-shots/
important-source.md

# Internal Robinhood opportunity/revenue doc (kept local, not for the public repo)
robinhood.md
robinhood-handoff.md

# Robinhood phase-0 working docs (tool inventory, intent-capture spec, compliance
# posture). Local only: they discuss a third-party unofficial client and are not for
# the public origin remote. Never added to docs.json, so they never publish either.
docs/robinhood-mcp-tools.md
docs/robinhood-intent-capture.md
docs/compliance-robinhood.md
docs/partner-grant-package.md
# Same reason: an outreach draft is go-to-market material, and this repo is public.
docs/robinhood-outreach.md

# Local design reference screenshots (never deployed, never committed)
_design-refs/
dashboard-examples/

# Third-party toolkit checked out locally for the Stellar work (kaankacar/stellar-build).
# It carries its own .git, so committing it would embed a foreign repo in ours.
stellar-build/
new-brand/

# Google Analytics screenshots for the Instawards evidence package (2026-09-05). Local only:
# they carry the browser chrome and are pasted into the Google Doc by hand.
/analytics.png
/rapor.png
/trafik-edinme.png
